// WIP: Building cart module


function getTotal() {

  // subTotal
  var subTotal = productPrice * quantity;
  // shipping
  var shipping; // flat, percent, or weight?
  // total
  var total = subTotal + shipping;
}



// WIP:
var itemPrice, quantity, subtotal, shipping, totalPrice;

  $(document).ready(function() {
      $(document).getElementById ( "shipping" ) {
        function totalPrice(subtotal, shipping) {
          $(document).getElementById( "ship") {
            function getTotal(subtotal, shipping) {
              var shipping  = 5.99;
              var totalPrice = subtotal + shipping;
              return "...";
      } // end ship
    } // end ready(function)
